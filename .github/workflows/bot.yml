name: Bot CI with Maven

on:
  push:
    paths:
      - .github/workflows/bot.yml
      - ru.tinkoff/java/bot**
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  packages: write
env:
  TOKEN: ${{secrets.TOKEN}}

jobs:  
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./ru.tinkoff/java
    steps:
      - name: Clone repo
        uses: actions/checkout@v3
        
      - name: List files
        run: ls -a
        
      - name: Build Maven project
        run: mvn package -pl bot -am

      - name: Build Docker image
        run: docker build-arg bot_token=$TOKEN --file bot.Dockerfile -t bot .
